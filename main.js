import React from 'react';
import { SafeAreaView } from 'react-native';
import WeatherApp from './WeatherApp';

const main = () => {
    return (
        <SafeAreaView>
            <WeatherApp />
        </SafeAreaView>
    );
};

export default main;